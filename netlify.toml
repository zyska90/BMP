[build]
  base = "apps/web"
  command = "cd ../.. && bun install --frozen-lockfile && cd apps/web && bun run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  BUN_VERSION = "1.1.4"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
