[build]
  base = "."
  command = "bun install --frozen-lockfile && bun run --cwd apps/web build"
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION = "20"
  BUN_VERSION = "1.1.4"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
